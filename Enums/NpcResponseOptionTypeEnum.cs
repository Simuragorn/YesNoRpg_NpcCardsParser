using System.ComponentModel.DataAnnotations;

namespace CardsExcelParser.Enums
{
    public enum NpcResponseOptionTypeEnum
    {
        [Display(Name = "Affirmative")]
        Affirmative,
        [Display(Name = "Negative")]
        Negative,
    }
}
